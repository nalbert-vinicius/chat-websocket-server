{
  "name": "chat-websocket-server",
  "version": "1.0.0",
  "description": "servidor with websocket for chat",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nalbert Vinicius",
  "license": "ISC",
  "dependencies": {
    "@types/socket.io": "^3.0.2",
    "express": "^4.17.1",
    "socket.io": "^4.1.3"
  }
}
